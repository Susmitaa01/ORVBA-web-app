import { AssistanceRequired } from './assistance-required.model';

describe('AssistanceRequired', () => {
  it('should create an instance', () => {
    expect(new AssistanceRequired()).toBeTruthy();
  });
});
